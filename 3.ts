let FirstName = "Kamran";
let LastName= "Munawar";
console.log(`Upper Case: ${FirstName.toUpperCase()} ${LastName.toUpperCase()}`);
console.log(`Lower Case: ${FirstName.toLowerCase()} ${LastName.toLowerCase()}`);
let titlecase1=(`Title Case: ${FirstName.charAt(0).toUpperCase()}${FirstName.slice(1)} ${LastName.charAt(0).toUpperCase()}${LastName.slice(1)}`)

console.log(titlecase1)

